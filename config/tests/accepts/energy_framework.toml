# Node with battery, named power states, and piecewise linear solar source.

[params]
timestep.length = 1
timestep.unit = "s"
timestep.count = 5
seed = 42
root = "~/simulations"

[nodes]

[nodes.main]
deployments = [{ charge = { max = 1000, quantity = 800, unit = "mwh" }, initial_state = "active" }]

[nodes.main.power_states]
sleep    = { rate = 20,  unit = "uw", time = "s" }
standby  = { rate = 200, unit = "uw", time = "s" }
active   = { rate = 80,  unit = "mw", time = "s" }

# Always-on solar panel generating 400 mW
[nodes.main.power_sources]
solar = { rate = 400, unit = "mw", time = "s" }

# MCU baseline draw
[nodes.main.power_sinks]
mcu = { rate = 10, unit = "mw", time = "s" }

[[nodes.main.protocols]]

name = "main"
runner = "python3"
runner_args = ["client.py"]
