use = ["boards/esp32_devkit"]

[params]
timestep.length = 1
timestep.unit = "s"
timestep.count = 5
seed = 42
root = "~/simulations"

[nodes.sensor]
profile = "nonexistent_profile"
deployments = [{}]

[[nodes.sensor.protocols]]
name = "firmware"
runner = "python3"
