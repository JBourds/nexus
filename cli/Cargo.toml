[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { path = "../config" }
runner = { path = "../runner" }
clap = { workspace = true, features = ["derive"] }
serde = { workspace = true, features = ["derive"] }
toml = { workspace = true }
anyhow = { workspace = true }
